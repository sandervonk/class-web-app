name: CI Tests

on:
    push:
        branches:
            - main

jobs:
    ci:
        runs-on: ubuntu-latest

        name: Run Lint and Test

        steps:
            - uses: actions/checkout@v3

            - uses: bahmutov/npm-install@v1

            - run: npm run lint

            - run: npm run test
